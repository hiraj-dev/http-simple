/** @type {import('next').NextConfig} */
const nextConfig = {
  // این تنظیم برای نسخه ۱۴ به بعد حذف شده
  // به جاش باید از serverActions bodySizeLimit استفاده کنی
  // اما برای middleware بدنه‌های بزرگ، باید از روش دیگه استفاده کنی
}

module.exports = nextConfig
